{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Paused repository: expose security fixes for human critical-only triage",
  "extends": ["security:only-security-updates"],
  "vulnerabilityAlerts": {
    "enabled": true,
    "addLabels": ["security"],
    "automerge": false
  }
}
